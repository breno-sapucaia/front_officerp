import React from 'react';

const Footer = (props) => {
    return ( 
        <footer className="footer d-flex align-items-center justify-content-center ">
            <small className="text-muted" style={{fontSize: "9px"}}>desenvolvido por edecoração</small>
        </footer>

    );
}
 
export default Footer;